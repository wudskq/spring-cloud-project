server:
  port: 3377

spring:
  application:
    name: cloud-alibaba-config-client3377
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
