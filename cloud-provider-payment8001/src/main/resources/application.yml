server:
  port: 8001

spring:
  application:
    name: cloud-prodiver-service
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/cloud_project_db?useUnicode=true&characterEncoding-utf-8&useSSL=false
    username: root
    password: root
    druid:
      test-while-idle: false

mybatis:
  mapperLocations: classpath*:mapper/*.xml